# Incomplete Report Example

This example intentionally omits environment details, expected behavior, actual behavior, and reproduction steps.

Run:

```sh
reprogate validate examples/incomplete-report/reprogate.yml
```

ReproGate should explain which information is missing and produce a low actionability score.
