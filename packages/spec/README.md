# @reprogate/spec

The specification package owns the ReproGate Evidence Manifest schema, TypeScript types,
JSON Schema export, deterministic normalization, SHA-256 hashing, receipt creation, and
rule-based actionability scoring.

The MVP supports schema version `0.1`.
